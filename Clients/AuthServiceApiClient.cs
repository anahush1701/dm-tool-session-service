namespace SessionService.Clients
{
    public class AuthServiceApiClient
    {
        private readonly HttpClient _httpClient;
        public AuthServiceApiClient(HttpClient httpClient)
        {
            _httpClient = httpClient;
        }

        //public async Task<User>
    }
}
